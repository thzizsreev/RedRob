# RedRob — unified dependencies (pipeline Stages 0–5 + both ONNX exports + optional honeypot study)
#
# Install once from repo root:
#   pip install -r requirements.txt
#
# Do NOT install onnxruntime (CPU-only) alongside onnxruntime-gpu.

# --- Core runtime (Stages 0–5) ---
onnxruntime-gpu>=1.16.0
transformers>=4.36.0
numpy>=1.24.0
faiss-cpu>=1.7.4
psutil>=5.9.0
protobuf>=3.20,<5
pyyaml>=6.0.0
polars>=0.20.0
lightgbm>=4.1.0
rank_bm25>=0.2.2
pyarrow>=15.0.0

# --- Stage 1 clustering (UMAP + HDBSCAN) ---
umap-learn>=0.5.5
hdbscan>=0.8.33
scikit-learn>=1.3.0
plotly>=5.18.0
pandas>=2.0.0

# --- ONNX export: INSTRUCTOR-large (onnx/export_to_onnx.py) ---
InstructorEmbedding>=1.0.1
torch>=2.0.0
onnx>=1.15.0

# --- ONNX export: cross-encoder MS MARCO (models/export_cross_encoder.py) ---
optimum[exporters-onnx]>=1.16.0
onnxscript>=0.1.0

# --- Optional: honeypot LLM research (honeypot/ — not used by run_pipeline.py) ---
openai>=1.40.0
python-dotenv>=1.0.0
