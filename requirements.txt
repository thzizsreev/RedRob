# Offline precompute — INSTRUCTOR-large ONNX (CUDA via onnxruntime-gpu)
onnxruntime-gpu>=1.16.0
transformers>=4.36.0
numpy>=1.24.0
faiss-cpu>=1.7.4
psutil>=5.9.0
protobuf>=3.20,<5

# Online rank + downstream
polars>=0.20.0
lightgbm>=4.1.0
rank_bm25>=0.2.2
pyarrow>=15.0.0

# Clustering test (test/run_clustering_test.py)
umap-learn>=0.5.5
hdbscan>=0.8.33
scikit-learn>=1.3.0
plotly>=5.18.0
pandas>=2.0.0

# NOTE: InstructorEmbedding + torch only needed for onnx/export_to_onnx.py (see onnx/requirements.txt)
# NOTE: do not install onnxruntime (CPU) alongside onnxruntime-gpu
