# Stage 3 runner — repeat for fusion/threshold tuning (no ONNX).

runner:
  precomputed_manifest: test_stage_3/precompute/artifacts/manifest.json

  per_query_k_dense: 200
  per_query_k_skill: 100
  miss_penalty_dense: 201
  miss_penalty_skill: 101

  rrf_k: 60
  alpha_neg: 0.5
  beta_cluster: 0.0
  z_threshold: 1.5
  min_k: 80
  max_k: 150

  output_dir: test_stage_3/runner/outputs
  expected_survivor_min: 400
  expected_survivor_max: 600
